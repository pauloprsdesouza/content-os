# AGENTS — Content OS

Use the lean pack first: [`.cursor/README.md`](.cursor/README.md).

1. `.cursor/sdd/system.md` → service → ADRs → feature
2. Apply `.cursor/rules/` (do not paste into plans)
3. Open `Content-OS-SDD-v1.0.md` only for deep detail (endpoint inventory, full state machines)

MVP: direct Compose deploy; CI/CD deferred (ADR-0016). Studio UI follows Figma `ws9h9RE7NGrE9hVbwqRnxY`.
